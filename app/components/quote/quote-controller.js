import QuoteService from "./quote-service.js";

let _qs = new QuoteService()


//there needs to be a draw to page function

export default class QuoteController {
  //we will call on _qs to get data from api
  //then we will draw to page

}
